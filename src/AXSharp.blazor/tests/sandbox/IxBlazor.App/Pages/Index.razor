@page "/"

<RenderableContentControl Presentation="Manual-Diagnostics"
                          Context="@Entry.Plc.MAINC.cu00">
</RenderableContentControl>

<div class="my-4 border-t border-gray-200"></div>

<RenderableContentControl Presentation="fdlkajfa"
                          Context="@Entry.Plc.MAINC.instanceOfstTest">
</RenderableContentControl>

<div class="my-4 border-t border-gray-200"></div>

<RenderableContentControl Presentation="Diagnostics"
                          Context="@Entry.Plc.MAINC.cuBase">
</RenderableContentControl>

<div class="my-4 border-t border-gray-200"></div>

<div class="bg-gray-50 rounded-lg p-8 mb-6">
    <div class="max-w-4xl mx-auto">
        <h1 class="text-4xl font-bold mb-4">Custom Hero Section</h1>
        <p class="text-lg text-gray-600 mb-6">Using a series of utilities, you can create this hero section, just like the one in previous versions. Check out the examples below for how you can remix and restyle it to your liking.</p>
        <button class="px-6 py-3 bg-blue-600 text-white text-lg rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50">
            Example button
        </button>
    </div>
</div>

<h1 class="text-3xl font-bold text-center mb-6">ixBlazor</h1>

<RenderableContentControl Presentation="control"
                          Context="@Entry.Plc.MAINC.groupBox_test">
</RenderableContentControl>

<div class="my-4 border-t border-gray-200"></div>

<!-- Button controls -->
<div class="space-x-4">
    <button class="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50"
            @onclick="ClickedStTest3">
        Button StTest3
    </button>

    <button class="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50"
            @onclick="ClickedIxComponent">
        Button IxComponent
    </button>
</div>

@if (AffectedObject != null)
{
    <RenderableContentControl Presentation="Manual"
                              Context="AffectedObject" />
}

<RenderableContentControl Presentation="Service"
                          Context="@Entry.Plc.MAINC.InstanceOfTwinComponent">
</RenderableContentControl>

<div class="my-4 border-t border-gray-200"></div>

<RenderableContentControl Presentation="Base"
                          Context="@Entry.Plc.MAINC.InstanceOfTwinComponent">
</RenderableContentControl>

<div class="my-4 border-t border-gray-200"></div>

<RenderableContentControl Context="@Entry.Plc.MAINC.InstanceOfTwinComponent">
</RenderableContentControl>

<div class="my-4 border-t border-gray-200"></div>

<RenderableContentControl Context="@Entry.Plc.MAINC.instanceOfstTest">
</RenderableContentControl>

<div class="my-4 border-t border-gray-200"></div>

<RenderableContentControl Context="@Entry.Plc.testingProgram.testEmpty">
</RenderableContentControl>

<div class="my-4 border-t border-gray-200"></div>

<RenderableContentControl Context="@Entry.Plc.testingProgram.testSimple" 
                          Presentation="Display">
</RenderableContentControl>

<div class="my-4 border-t border-gray-200"></div>

<RenderableContentControl Context="@Entry.Plc.MAINC" 
                          Presentation="Display">
</RenderableContentControl>

@code
{
    private ITwinObject AffectedObject { get; set; } = null;
    private string Presentation { get; set; }

    public void ClickedStTest3()
    {
        AffectedObject = @Entry.Plc.MAINC.instanceOfIxComponent;
    }

    public void ClickedIxComponent()
    {
        AffectedObject = @Entry.Plc.MAINC.instanceOfstTest3;
    }
}
